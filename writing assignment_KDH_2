\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{kotex}

\renewcommand{\baselinestretch}{1.5}
\setlength{\oddsidemargin}{.5in}
\setlength{\evensidemargin}{1.5pt}

\usepackage{amsmath,amssymb,amsthm} %\usepackage{amscd}
\usepackage{latexsym}
\usepackage{fancyhdr}
\usepackage{float,srcltx,epsfig}%for .eps file
\usepackage{multirow}
\usepackage{enumerate}
\usepackage{mathrsfs}
\usepackage{algorithm}
\usepackage{amsfonts}
\makeindex %
\newenvironment{explain}{\begin{quote}}{\hfill $\square$\end{quote}}
\newenvironment{glos}[1]{\begin{quote}$\quad$\textbf{#1}\\}{\end{quote}}
\newenvironment{problem}[1]{\begin{quote}$\quad$\textbf{#1}\\}{\end{quote}}
\newenvironment{theorem}[1]{\begin{quote}$\quad$\textbf{#1}\\}{\end{quote}}


\newcommand{\myindex}[1]{#1\index{#1}}
\newcommand{\mi}[1]{#1\index{#1}}
\newcommand{\SK}{\hspace{0.3cm}}

%\newcommand{\Z}{{\mathbb Z}} 
%\newcommand{\Q}{{\mathbb Q}}
%\newcommand{\F}{{\mathbb F}}
%\newcommand{\R}{{\mathbb R}}
%\newcommand{\C}{{\mathbb C}}

\newcommand{\set}[1]{\left\{#1\right\}}

\newcommand{\ord}{{\operatorname{ord }}}
\newcommand{\ind}{{\operatorname{ind }}}
\newcommand{\rank}{{\operatorname{Rank}}}

\pagestyle{myheadings}   %
\pagestyle{fancyplain}
\fancyhf{}% clear all fields
%\fancyhead[LE,RO]{\bfseries\thepage}
%\fancyhead[LO]{\bfseries\tiny\rightmark}
%\fancyfoot[RO]{\textsf{\small{Copyright \copyright 2018 Jung Hee Cheon.}}}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\def\BigRoman{\uppercase\expandafter{\romannumeral\number\count 255 }}
\def\Romannumeral{\afterassignment\BigRoman\count255=}
\title{Tate cohomology group}
\author{Donghyun Kim }
\date{April 2019}

\begin{document}

\maketitle

Last time, I wrote about group cohomology and Shapiro's lemma. In this document, I will define Tate cohomology group.

Likewise cohomology case, we can define homology of group.

Let G be a group, $\Lambda$ be a group ring, A,B be G-modules and $A \otimes B$ be tensor product of A and B.

To give a G-module structure on $A \otimes B$, we should define $g(a \otimes b) = (ga) \otimes (gb)$

Let $I_{G}$ be the kernel of the homomorphism $\Lambda$ to $\mathbb{Z}$ which maps each element of G to 1.

By right-exactness of tensor functor, we get $\mathbb{Z}\otimes _{G}A \simeq A/I_{G}A$

$A/I_{G}A$ is denoted by $A_{G}$ and it corresponds to $A^{G}$ in cohomology case.

Using tensor product instead of Hom functor, homology is similarly defined.

If the group G is finite, we can combine cohomology and homology. 

It is called Tate Group.

Let G be a finite group and N be a $\sum_{s\in G} s $.

For any G-module A, multiplication by N defines an endomorphism of A.

Clearly, $I_{G}A \subseteq Ker(N), Im N \subseteq A^{G}$.

Thus N induces a homomorphism $N^{*}:H_{0}(G,A) \rightarrow H^{0}(G,A) $

We can define $\hat{H}_{0}(G,A) = Ker (N^{*}), \hat{H}^{0}(G,A) = Coker(N^{*})$

Now Tate group is defined as

$\hat{H}^{q}(G,A) = H^{q}(G,A)$ for $q \geq 1$

$\hat{H}^{-1}(G,A)=\hat{H}_{0}(G,A)$

$\hat{H}^{-q}(G,A) = H_{q-1}(G,A)$ for $q \geq 2$

In case of finite group, induced and co-induced module has same extension.

Because mapping $Hom(\Lambda,X) \rightarrow \Lambda \otimes X$

$\phi \mapsto \sum_{s \in G} s \otimes \phi(s)$ is G-module isomorphism.

If A is an induced G-module(or equivalently co-induced), 

$\hat{H}_{0}(G,A) = 0, \hat{H}^{0}(G,A) = 0$


\end{document}
